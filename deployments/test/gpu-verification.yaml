apiVersion: v1
kind: Pod
metadata:
  name: gpu-verification
spec:
  restartPolicy: OnFailure
  containers:
  - name: runtime-container
    image: ubuntu
    imagePullPolicy: IfNotPresent
    command: ["nvidia-smi"]
    resources:
      limits:
        nvidia.com/gpu: 1